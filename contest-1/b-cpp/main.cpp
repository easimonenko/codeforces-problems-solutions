#include <iostream>
using namespace std;

struct Coord
{
	int row;
	int column;
	Coord(string input) {
		if (input[0] == 'R' && isdigit(input[1])) {
			int idxOfC = -1;

		}
	}
};

int main (int argc, char *argv[])
{
	int k;
	cin >> k;
	while (k > 0) {
		string input;
		Coord coord(input);
		k--;
	}
	
	return 0;
}

