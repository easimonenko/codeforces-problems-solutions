#include <iostream>

using namespace std;

int main() {
  int a, b, n, d;
  cin >> a >> b >> n >> d;

  int ans_num = -1;
  long long ans_s = 0;

  for (int k = 0; k < n; k++) {
    int x, y;
    cin >> x >> y;

  }

  cout << ans_num << ans_s << endl;

  return 0;
}
